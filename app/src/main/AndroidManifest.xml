<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dscddu.dscddu">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dsc_new"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dsc_new"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ForgetPasswordActivity" android:theme="@style/PlainTheme"
            android:parentActivityName=".Activities.LoginActivity"></activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:parentActivityName=".Activities.SignUpActivity"
            android:theme="@style/PlainTheme"></activity>
        <activity
            android:name=".Activities.SignUpActivity"
            android:theme="@style/PlainTheme" />
        <activity
            android:name=".Activities.OnBoardingActivity"
            android:theme="@style/BlueTheme" />
        <activity
            android:name=".Activities.SplashScreen"
            android:theme="@style/BlueTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/PlainTheme" />
    </application>

</manifest>